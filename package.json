{
  "name": "interval-call",
  "version": "0.4.0",
  "description": "Prevents the same function called repeatedly within a certain interval time.",
  "keywords": [
    "interval-call"
  ],
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/min9nim/interval-call.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --declaration --outDir dist",
    "test": "mocha -r ts-node/register test/**/*.spec.ts",
    "dev": "node test.js"
  },
  "author": "min9nim <min9nim@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/es6-shim": "^0.31.39",
    "@types/mocha": "^5.2.7",
    "@types/node": "^13.5.0",
    "ts-node": "^8.5.4",
    "tslib": "^1.10.0",
    "typescript": "^3.7.4",
    "chai": "^4.2.0",
    "mocha": "^6.2.2"
  }
}
